import { Injectable, UnauthorizedException, Logger } from '@nestjs/common';\nimport { AuthGuard } from '@nestjs/passport';\n\n/**\n * Guard JWT para proteger rotas\n * Toda rota decorada com @UseGuards(JwtAuthGuard) exige token válido no header\n * Exemplo: Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...\n */\n@Injectable()\nexport class JwtAuthGuard extends AuthGuard('jwt') {\n  private readonly logger = new Logger(JwtAuthGuard.name);\n\n  handleRequest(err, user, info) {\n    if (err || !user) {\n      this.logger.warn(`Acesso não autorizado: ${info?.message || err?.message}`);\n      throw new UnauthorizedException({\n        statusCode: 401,\n        message: 'Token não fornecido ou inválido',\n        error: 'Unauthorized',\n      });\n    }\n    return user;\n  }\n}\n